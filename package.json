{
  "name": "smart-cicd",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run --config vitest.config.e2e.ts",
    "test:e2e:watch": "vitest --config vitest.config.e2e.ts",
    "typecheck": "tsc --noEmit",
    "release:version": "changeset version",
    "release:publish": "echo 'Publishing to GitHub Releases only - no npm publish'"
  },
  "dependencies": {
    "@kubernetes/client-node": "^0.22.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
