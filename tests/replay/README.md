# Replay Tests

Replay tests for event-driven behavior: event stream processing, log-driven failure detection, intervention triggering, repeated-error scenarios, stuck detection.

Run with: `npm test -- --testPathPattern=tests/replay`
