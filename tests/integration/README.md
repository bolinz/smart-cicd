# Integration Tests

Integration tests for component interaction: orchestrator + watcher, runner lifecycle, action execution boundaries, live status propagation.

Run with: `npm test -- --testPathPattern=tests/integration`
