# UI

Live run view service. Provides real-time visibility into active pipeline runs: current stage, active step, recent key events, current diagnosis, action history, and risk level.

## Boundaries

- Read-only view of run state; does not mutate runs or execute actions
- First-class product surface, not an afterthought
